<%= simple_form_for([@idea, @idea.comments.build], remote: true) do |f| %>
  <%= f.input :comment, :input_html => { :maxlength => 255 }, hint: 'Maximum limit of 255 characters allowed.' %>
  <%= f.submit %>
<% end %>